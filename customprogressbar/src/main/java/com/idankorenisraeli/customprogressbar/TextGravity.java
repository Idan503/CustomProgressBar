package com.idankorenisraeli.customprogressbar;


/**
 * Horizontal placement of text in the bar.
 * Vertically it will always be in a center gravity.
 */
public enum TextGravity {
    CENTER,
    START,
    END,
    LEFT,
    RIGHT,
    STICK_BAR // Sticking to bar progression while keeping itself inside the view
}
